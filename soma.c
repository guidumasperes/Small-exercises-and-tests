#include <stdio.h>
#include <stdlib.h>

int main (int argc, char* argv[]) {
	int numero1, numero2, soma;
	scanf("%d %d", &numero1, &numero2);
	soma = numero1 + numero2;
	printf("%d", soma);
	system("pause");
	return 0;
	
}
	
