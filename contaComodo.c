#include<stdio.h>

int main (void) {
	int i, j, n;
	char matriz[n][n];
	
	scanf("%i", &n);
	for(i = 0; i < n; i++) {
		for(j = 0; j < n; j++) {
			scanf("%c", &matriz[i][j])
		}
	}
	achaComodo(matriz, n);
	return 0;
}

void achaComodo(char m[][n], int n) {
	
}
